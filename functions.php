<?php

if (!defined('FV_THEME_URL')) {
    define('FV_THEME_URL', get_stylesheet_directory_uri());
}

if (!defined('FV_THEME_PATH')) {
    define('FV_THEME_PATH', get_stylesheet_directory());
}

require_once('inc/init.php');